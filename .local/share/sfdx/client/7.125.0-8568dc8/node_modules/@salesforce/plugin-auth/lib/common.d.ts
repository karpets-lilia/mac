import { AuthInfo, AuthFields } from '@salesforce/core';
import { Optional } from '@salesforce/ts-types';
interface Flags {
    setalias?: string;
    setdefaultdevhubusername?: boolean;
    setdefaultusername?: boolean;
}
export declare class Common {
    static handleSideEffects(authInfo: AuthInfo, flags: Flags): Promise<void>;
    static resolveLoginUrl(instanceUrl: Optional<string>): Promise<Optional<string>>;
    static identifyPossibleScratchOrgs(fields: AuthFields, orgAuthInfo: AuthInfo): Promise<void>;
    static getDevHubAuthInfos(): Promise<AuthInfo[]>;
}
export {};
