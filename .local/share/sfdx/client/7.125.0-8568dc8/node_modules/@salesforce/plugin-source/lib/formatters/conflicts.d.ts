import { UX } from '@salesforce/command';
import { ChangeResult } from '@salesforce/source-tracking';
/**
 * Write a table (if not json) and throw an error that includes a custom message and the conflict data
 *
 * @param conflicts
 * @param ux
 * @param message
 */
export declare const processConflicts: (conflicts: ChangeResult[], ux: UX, message: string) => void;
